@model IEnumerable<FADemo.Models.BaseInformation.AssetType>

@{
    ViewData["Title"] = "AssetTypeIndex";
}

<h1>AssetTypeIndex</h1>

<p>
    <a asp-controller="BaseInformations" asp-action="AssetTypeCreate">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssetTypeNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetTypeCreateDatetime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetTypeDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetTypeIsDisabled)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AssetTypeNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AssetTypeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AssetTypeCreateDatetime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AssetTypeDescription)
                </td>
                <td>
                    @*@Html.CheckBoxFor(modelItem => item.IsDisabled)  *@
                    @*                    <form asp-action="FA_AssetTypeDisable" method="POST">
                <input type="checkbox" class="done-checkbox">
                <input type="hidden" name="id" value="@item.Id">
                </form>*@
                    @if (item.AssetTypeIsDisabled == false)
                    {
                        <a asp-action="AssetTypeDisable" asp-route-id="@item.AssetTypeId" class="btn btn-danger">Disable</a>
                    }
                    else
                    {
                        <a asp-action="AssetTypeDisable" asp-route-id="@item.AssetTypeId" class="btn btn-primary">Enable</a>
                    }

                </td>
                @*            <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>*@
            </tr>
        }
    </tbody>
</table>
